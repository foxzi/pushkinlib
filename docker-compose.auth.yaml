# Override for running with authentication enabled.
# Usage: docker compose -f docker-compose.dev.yaml -f docker-compose.auth.yaml up -d --build
services:
  pushkinlib:
    environment:
      - AUTH_ENABLED=true
      - ADMIN_USER=admin
      - ADMIN_PASS=admin123
      - SESSION_SECRET=dev-session-secret-change-in-production
